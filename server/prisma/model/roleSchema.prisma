model Role {
  id          String   @id @default(uuid())
  name        String   @unique // Admin, FleetManager, Dispatcher, Driver, Viewer
  permissions Json     // e.g. ["vehicle:create","trip:dispatch","report:view"]
  users       User[]
}